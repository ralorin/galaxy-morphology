# PyTorch is installed separately in setup_env.sh from the CUDA 12.1 wheel index,
# so it is deliberately not pinned here (pip would otherwise pull the CPU build).
timm==1.0.11
numpy==1.26.4
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.5.2
h5py==3.12.1
pillow==11.0.0
opencv-python-headless==4.10.0.84
matplotlib==3.9.2
seaborn==0.13.2
statsmodels==0.14.4
tqdm==4.66.5
requests==2.32.3
